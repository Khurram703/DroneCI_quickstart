---
kind: pipeline
name: test-on-amd64

platform:
  arch: amd64

steps:
- name: test
  image: python
  commands:
  - pip install -r requirements.txt
  - pip install -U pytest
  - pytest --version
  - pytest

---
kind: pipeline
name: test-on-arm64

platform:
  arch: arm64

steps:
- name: test
  image: python
  commands:
  - pip install -r requirements.txt
  - pip install -U pytest
  - pytest --version
  - pytest
  
---
kind: pipeline
name: python-2

steps:
- name: test
  image: python:2
  commands:
  - pip install -r requirements.txt
  - pip install -U pytest
  - pytest --version
  - pytest
